import { FC } from 'react'
import styles from './GroupSelectionWindow.module.css'

const GroupSelectionWindow: FC = () => {
    return (
        <div className={styles.wrapper}>
            будет доступно в альфа-версии
        </div>
    )
}

export default GroupSelectionWindow